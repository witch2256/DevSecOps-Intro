metadata:
  id: CKV2_CUSTOM_1
  name: Ensure S3 bucket has lifecycle configuration
  category: GENERAL_SECURITY
  severity: HIGH
definition:
  and:
    - cond_type: attribute
      resource_types:
        - aws_s3_bucket
      attribute: lifecycle_rule
      operator: exists
